
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All paths are locked down by default.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
